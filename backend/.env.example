PORT=5000
MONGO_URI=mongodburl
# Path to ffmpeg/ffprobe binaries if not on system PATH (optional — the Docker
# image already has ffmpeg on PATH, so leave blank on Render)
FFMPEG_PATH=
FFPROBE_PATH=
# Your deployed frontend URL(s), comma-separated. Leave blank for local dev.
CLIENT_ORIGIN=
